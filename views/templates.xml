<odoo>
    <!--
    <data>
        <record id="kanban_view" model="ir.ui.view">
            <field name="name">technical.support.kanban</field>
            <field name="model">technical.support</field>
            <field name="arch" type="xml">
                <kanban>
                    <field name="name"/>
                    <field name="claim_type"/>
                    <field name="company"/>
                    <field name="plan"/>
                </kanban>

            </field>

            <template id="listing">
            </template>
        </record>

    </data>
    -->
</odoo>